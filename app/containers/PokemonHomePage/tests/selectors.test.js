// import { selectEmployeeListPageDomain } from '../selectors';

describe('selectEmployeeListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
